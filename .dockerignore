# Keep Docker context minimal and explicit
.git
.github
.vscode

# Build outputs and temp files
bin
# Exclude only a root-level built binary, not cmd/porttrap source directory
/porttrap
/porttrap.exe
*.log
*.tmp

# Docs and examples are not needed for image build
examples
README.md
