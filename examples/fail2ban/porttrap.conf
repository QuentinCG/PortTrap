[Definition]
# Fail2Ban filter for PortTrap
# Supports both text lines and JSON lines emitted by PortTrap.
failregex = ^\s*(TCP|UDP) <HOST>:\d+ -> \d+\s*$
            ^\{.*"source_ip"\s*:\s*"<HOST>".*\}\s*$

ignoreregex =
