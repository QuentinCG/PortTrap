name: CI + Docker image

# Triggers:
# - push on main: run tests + build/push image (version comes from internal/version/VERSION)
# - pull_request on main: run tests only
# - schedule: monthly run (UTC)
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # At 03:17 UTC on day-of-month 1.
    - cron: '17 3 1 * *'

jobs:
  # Fast CI gate: run Go tests before any image build/push.
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          # Use the exact version declared in go.mod to keep CI and code aligned.
          go-version-file: go.mod

      - name: Run tests
        run: |
          go test ./... -v

  # Docker packaging job:
  # - waits for tests to pass
  # - runs on push and scheduled events (not on pull requests)
  docker:
    needs: test
    if: github.event_name == 'push' || github.event_name == 'schedule'
    runs-on: ubuntu-latest
    permissions:
      # Required to pull source and push image to GHCR.
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Normalize registry owner to lowercase
        id: vars
        run: |
          echo "owner_lc=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Read version
        id: ver
        run: |
          v="$(tr -d '[:space:]' < internal/version/VERSION)"
          echo "major=${v%%.*}" >> "$GITHUB_OUTPUT"   # 1.0.0 -> 1
          echo "minor=${v%.*}" >> "$GITHUB_OUTPUT"    # 1.0.0 -> 1.0

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          # push=true because this job only runs on push events.
          push: true
          # Publish multi-arch image.
          platforms: linux/amd64,linux/arm64
          # Tags derived from internal/version/VERSION: latest, major and major.minor.
          # The full patch tag (e.g. 1.0.0) is intentionally omitted: it adds little value
          # over major.minor and keeps the number of stored image versions down.
          tags: |
            ghcr.io/${{ steps.vars.outputs.owner_lc }}/porttrap:latest
            ghcr.io/${{ steps.vars.outputs.owner_lc }}/porttrap:${{ steps.ver.outputs.major }}
            ghcr.io/${{ steps.vars.outputs.owner_lc }}/porttrap:${{ steps.ver.outputs.minor }}

      - name: Delete old GHCR versions
        uses: actions/delete-package-versions@v5
        with:
          package-name: porttrap
          package-type: container
          min-versions-to-keep: 5
          delete-only-untagged-versions: true
